{
  "name": "wrangler-test",
  "version": "0.0.0",
  "devDependencies": {
    "vitest": "^0.29.7",
    "vitest-environment-miniflare": "^2.12.1",
    "wrangler": "2.12.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "test": "vitest"
  }
}
